## SnapFace Back-end | Development server

Run `npm install` for install dependencies. Run `npm run start` for launch the server and navigate to `http://localhost:3000/facesnaps` to see the data of `db.json`.
